# Copy this file to .env and fill in real values for local development.
# Never commit .env — it is gitignored.

# --- Database (Phase 2) ---------------------------------------------------
# PostgreSQL connection string. Local dev / staging / production each get
# their own value; this is a placeholder only.
DATABASE_URL="postgresql://user:password@localhost:5432/kwanomzi?schema=public"

# --- Auth (Phase 3) --------------------------------------------------------
# AUTH_SECRET=

# --- Yoco payments (Phase 8) ------------------------------------------------
# YOCO_SECRET_KEY=
# YOCO_WEBHOOK_SECRET=
# YOCO_MODE=test
